import * as React from "react";

export interface BulletinProps { }

const Bulletin: React.SFC<BulletinProps> = () => {
    return (
        <div>
            This is the Bulletin Module
        </div>
    );
};

export default Bulletin;
