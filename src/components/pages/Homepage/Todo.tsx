import * as React from "react";

export interface TodoProps { }

const Todo: React.SFC<TodoProps> = () => {
    return (
        <div>
            This is the Todo module
        </div>
    );
};

export default Todo;